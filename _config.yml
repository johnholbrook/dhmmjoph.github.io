defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "pages" # previously `post` in Jekyll 2.2.
    values:
      layout: "default"
  -
  	scope:
  	  path: "LEGO"
  	  type: "pages"
  	values:
  	  layout: "LEGO"	